# Review — 20260726T141411Z

| field | value |
|---|---|
| tool | `codex-cli 0.145.0` exec, REVIEW_GATE.md priority order |
| model | gpt-5.6-sol |
| reasoning effort | high |
| sandbox | read-only |
| scope | whole repository at HEAD 1f0f670 |
| branch | master |
| requested by | Kemal, 2026-07-26 |

This file is EVIDENCE: never edit it after the fact. The findings below are
unverified input — verification notes live in RESEARCH_LOG.md.

---

## Findings

1. **The placeholder gate makes a normally configured project permanently red.**  
   [check.sh:140](core/scripts/check.sh:140) scans every tracked/unignored file for literal tokens. But the installed, kit-owned [INTERVIEW.md:16](setup/INTERVIEW.md:16) contains `{{PLACEHOLDER}}`, its tables contain every real token, and the kit-owned [MODULE_AGENTS_TEMPLATE.md:3](core/MODULE_AGENTS_TEMPLATE.md:3) deliberately retains `{{MODULE}}`. The interview explicitly says to fill copies of that template, not the template itself ([INTERVIEW.md:169](setup/INTERVIEW.md:169)). A fresh install therefore cannot pass without editing or deleting kit-owned documentation. Worse, [sync-kit.sh:57](sync-kit.sh:57) later overwrites those files and reintroduces the tokens. Static enumeration finds 34 distinct tokens in `core/` and `setup/`, not the 33 claimed at [ACCEPTANCE.md:22](docs/ACCEPTANCE.md:22).

2. **Placeholder scanning converts scanner failure into “no findings.”**  
   [check.sh:45](/mnt/d/_Production_D/Development/Projects/_UnityProjects/_My_Projects/_UnityProjects/_My_Projects/MyAgentKit/core/scripts/check.sh:45) puts `files_to_scan` on the non-final side of a pipeline and ends the entire pipeline with `|| true`. A failed `git ls-files`, failed fallback `find`, missing/incompatible `xargs`, unreadable file, or failed `grep` is indistinguishable from zero matches. The gate then reaches `CHECK: PASS`. A read-only reproduction with a list producer returning status 7 produced pipeline status 0.

3. **The placeholder scanner mishandles legal filenames.**  
   The newline list is converted to NUL only after filenames have already been separated at [check.sh:45](core/scripts/check.sh:45). A filename containing a newline is split or Git-quoted, the resulting nonexistent paths are silently discarded, and its token is missed. There is also no `--` before filename arguments: in a Git repository a tracked file named `-v` is passed to GNU `grep` as an option after `/dev/null`, so the file is never scanned. Ordinary spaces happen to survive; newline and option-shaped names do not.

4. **Project boundary enforcement is optional despite being documented as mandatory.**  
   [check.sh:187](core/scripts/check.sh:187) silently skips a missing `boundary_checks.sh`; an empty or comments-only file also passes. [check.sh:115](core/scripts/check.sh:115) similarly skips missing boundary self-tests. The script can therefore print both `CHECK: PASS` and `SELF-TEST: PASS` with no boundary enforcement or negative tests, contradicting “not optional” at [INTERVIEW.md:153](setup/INTERVIEW.md:153).

5. **The build/test gate explicitly passes while running nothing.**  
   An empty configured command only emits a warning at [check.sh:199](core/scripts/check.sh:199); `fail` remains zero. The comment says this becomes a failure after code lands, but no code-presence condition implements that transition. A project can consequently retain an empty command indefinitely and report `CHECK: PASS`, directly contradicting the absent-tool rule at [AGENTS.md:84](core/AGENTS.md:84).

6. **Deleting or damaging `STATE.md` disables its gate.**  
   The entire rot check is conditional on both file existence and an exact heading match at [check.sh:156](core/scripts/check.sh:156). A missing file, renamed heading, or unreadable file silently skips the check. A thousand-line state file with no exact `## Active work` heading passes. Even with the heading, any stale bullet—including “done” or “nothing active”—counts as real work and suppresses failure.

7. **`--self-test` does not prove what its PASS message claims.**  
   The final message says every gate was observed rejecting its failure case at [check.sh:120](core/scripts/check.sh:120), but:

   - There is no negative test for the build/test gate.
   - Boundary tests are optional.
   - A boundary test treats any nonzero result from the whole gate as proof. The injected file can break compilation or another scanner while the intended boundary check remains dead.
   - The baseline branch at [check.sh:60](core/scripts/check.sh:60) labels every invocation error—wrong `$0`, missing interpreter, environmental build failure—as “the gate is already failing on this tree.” It establishes only that the baseline command returned nonzero, not why.

   Thus a PASS proves that certain injections made the aggregate script nonzero, not that each named gate fired for its claimed reason.

8. **`review.sh` repeats the repository’s documented historical empty-diff bug.**  
   [review.sh:94](core/scripts/review.sh:94) exits 0 when the collected diff is empty. That directly contradicts [WORKFLOW.md:60](core/docs/WORKFLOW.md:60), which names an empty diff as a mandatory failure, and [RESEARCH_LOG.md:51](RESEARCH_LOG.md:51), which describes exactly this behavior as a founding fail-open defect.

9. **`review.sh` can succeed with no review evidence and no archive.**  
   If the CLI exits 0 without writing its final-output file, [review.sh:175](core/scripts/review.sh:175) merely embeds a transcript and the script exits 0. It never requires a verdict or even a nonempty message. Separately, failure to create `docs/reviews`, write the report at [review.sh:184](core/scripts/review.sh:184), or read it back is ignored; [review.sh:188](core/scripts/review.sh:188) exits only with the CLI’s status. In an unwritable repository, a successful CLI invocation can therefore produce no durable evidence while the wrapper reports success.

10. **The Unity gate accepts fabricated or entirely skipped tests as execution evidence.**  
    [unity_gate.sh:65](overlays/unity/files/scripts/unity_gate.sh:65) counts raw `<test-case ` substrings without inspecting result status or validating XML. All-skipped test cases count as executed. A stub that exits 0 and writes one such substring to each requested result path makes the gate pass without executing Unity or a test. Its self-test at [unity_gate.sh:85](overlays/unity/files/scripts/unity_gate.sh:85) tests only absent files, so it does not catch this.

11. **The Unity zero-test path crashes before its intended diagnostic.**  
    `grep -c` prints `0` and returns 1 on no matches, after which `|| echo 0` appends a second zero at [unity_gate.sh:65](overlays/unity/files/scripts/unity_gate.sh:65). `n` becomes `0\n0`; dash aborts arithmetic at line 67 with `Illegal number`. This was reproduced read-only. The advertised “no test executed” check at line 73 is not reached for an existing zero-case result file.

12. **Retrofitting can finish successfully with a no-op commit gate.**  
    [bootstrap.sh:62](bootstrap.sh:62) skips every existing file, including `scripts/check.sh` and `.githooks/pre-commit`, while [bootstrap.sh:105](bootstrap.sh:105) still wires `core.hooksPath` and exits successfully. In a live repository where either path already contains a no-op, the installer reports completion while installing no enforcement. The skip list is informational rather than a hard stop.

13. **The tool-neutrality framing is false for the shipped automation.**  
    [review.sh:19](core/scripts/review.sh:19) claims environment variables prevent vendor hardcoding, but the script hardcodes a Codex path/default at lines 44–45 and Codex-specific `exec -m -s -c -o` syntax at [review.sh:132](core/scripts/review.sh:132). Setting `REVIEW_CLI_BIN` to Claude or another CLI does not neutralize that contract; it just sends Codex flags to the other program. The default installation also ships substantial `.claude` hooks, agents, and skills. For example, [diff-reviewer.md:13](core/.claude/agents/diff-reviewer.md:13) repeats operative review policy rather than being a one-line pointer, contradicting the universal claim at [one-canonical-instruction-file.md:11](docs/one-canonical-instruction-file.md:11). The policy documents can be used manually by other tools; the supplied automation is Codex-shaped and Claude-augmented.

14. **“Local PASS == CI PASS” is disproved inside the repository itself.**  
    [AGENTS.md:23](core/AGENTS.md:23), the hook, and check-script header state this equality. But [GOTCHAS.md:29](core/docs/GOTCHAS.md:29) documents a concrete counterexample: on a filesystem with `core.filemode=false`, running the script locally can pass while CI’s `./scripts/check.sh` fails with exit 126 because the executable bit was not recorded. Same script does not mean equivalent environment or result.

15. **The POSIX portability claim is broader than the implementation.**  
    The scripts parse under dash, but they depend on non-POSIX utility extensions: `xargs -0` at [check.sh:45](core/scripts/check.sh:45), recursive `grep -r` at [sync-kit.sh:57](sync-kit.sh:57), `mktemp`, `trap ... EXIT`, and obsolete `head -1`/`tail -5` forms. These are not necessarily GNU-only—common BSD implementations support several—but they are not guaranteed by POSIX. Critically, lack of `xargs -0` becomes a silent green placeholder scan because its error is discarded. `sync-kit.sh` also splits `grep` results through `for src in $(...)`, so a kit-owned filename containing spaces is broken into multiple paths.

16. **Acceptance evidence is already inconsistent with the current tree.**  
    [ACCEPTANCE.md:13](docs/ACCEPTANCE.md:13) records 27 copied files, but the current bootstrap inputs contain 27 files under `core/` plus two under `setup/`: 29 copied files. Its statement that `--self-test` proves “each gate” at line 25 is also false because build/test is untested and boundary tests are optional. README consequently overclaims that “the gates are verified to fail when they should” at [README.md:10](README.md:10), while ACCEPTANCE itself admits that `review.sh` has never had a live run.

17. **The architecture template contradicts itself about unenforced boundaries.**  
    [ARCHITECTURE.md:46](core/docs/ARCHITECTURE.md:46) states that every listed boundary is enforced by something that fails a build. Its own instruction at [ARCHITECTURE.md:51](core/docs/ARCHITECTURE.md:51), and the interview at line 144, explicitly permit entries marked “not enforced.” Both cannot describe the resulting section truthfully.

No repository files or state were changed. The executed diagnostics were limited to reads, placeholder enumeration, `dash -n`, and non-mutating shell behavior probes.

VERDICT: Reject